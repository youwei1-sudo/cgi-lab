q1 How do you inspect all environment variables in Python
import os
dict(os.environ),

q2 What environment variable contains the query parameter data
QUERY_STRING

q3 What environment variable contains information about the user’s browser?

in the HTTP_USER_AGENT	,  

q4 : How does the POSTed data come to the CGI script
The system will read the posted_bytes, sys.stdin.read(int(posted_bytes))? 

q5: What is the HTTP header syntax to set a cookie from the server?
Set-Cookie: <cookie-name>=<cookie-value>
While, in the code i used, print ("set-cookie: session_id = ",  session_id), in order to add it to the header

q6:What is the HTTP header syntax the browser uses to send the cookie back?
Cookie: cookie-name=cookie-value; 
And if there are multiple cookie be setted, then all cookies will be seperated by ; 
For my case, Cookie: session_id=this-is-deprecated; username=David; password=t; log_in=True

